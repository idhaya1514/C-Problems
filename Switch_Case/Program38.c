#include <stdio.h>

int main() {
        int type;
        scanf("%d",&type);
        switch(type)
        {
             case 1:
                printf("Consultation_Fee -500");
                break;
            case 2 : 
                printf("Consultation_Fee - 1200");
                break;
            default:
                printf("Invalid Inputs");
              } return 0;
        
        
}
